INSTALLATION DU HOOK PRE-COMMIT

1. Copiez le fichier `hooks/pre-commit` dans le répertoire `.git/hooks/` :
   cp hooks/pre-commit .git/hooks/

2. Testez le hook en effectuant un commit :
   git commit -m "Test du hook pre-commit"


